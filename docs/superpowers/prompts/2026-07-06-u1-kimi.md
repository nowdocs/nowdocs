# Kimi implementation prompt — U1 `nowdocs smoke` + first-run UX

You are implementing **U1 only** for nowdocs. Do not implement R3 doctor, R4 cache repair, or registry discovery.

Difficulty: **4/10 to 5/10 (medium)**. The hard parts are real retrieval smoke behavior, model first-run messaging, and stable CLI output tests.

## Worktree requirement

Work in an independent git worktree and stage-specific branch:

```bash
git worktree add ../nowdocs-u1-smoke-ux -b feat/u1-smoke-ux
cd ../nowdocs-u1-smoke-ux
```

Do not implement from a dirty shared worktree. Do not reuse another agent's branch.

## Read first

1. `AGENTS.md`
2. `docs/superpowers/specs/2026-07-06-robustness-ux.md`
3. `docs/superpowers/plans/2026-07-06-robustness-ux-plan.md`
4. `docs/superpowers/specs/2026-07-06-u1-smoke-ux.md`
5. R2 implementation diff/commit; R3 if already landed

## Task

Implement `nowdocs smoke` and improve first-run command success outputs.

## Scope and review risk

Primary files:

- `src/cli.rs`
- `src/main.rs`
- `tests/cli_tests.rs`
- `tests/retrieve_tests.rs`
- `README.md` only if needed for command help examples

Review risk is medium around:

- not changing retrieval ranking.
- not adding network-dependent tests.
- model first-run errors being actionable.
- preserving existing CLI test substrings.
- JSON output being parseable.

## Requirements

1. Add `Smoke` CLI subcommand with `<docset> [query] [--json] [--top-k <n>]`.
2. Smoke must run real retrieval, not shallow structural checks.
3. Human output includes docset, query, elapsed time, count, and top results.
4. JSON output includes docset, query, elapsed_ms, result_count, results.
5. Missing/invalid docsets exit non-zero with actionable hint.
6. Improve install/update/ingest/share/list-installed output with next-step hints where practical.
7. Do not introduce network-dependent default tests.

## TDD checklist

Write failing tests first:

- help lists smoke.
- smoke invalid docset fails.
- smoke missing docset fails with hint.
- smoke fixture returns result output.
- smoke `--json` parses.
- install/ingest/share success output includes next command.
- list-installed remains deterministic.

## Verification

Run:

```bash
cargo test cli_tests -- --test-threads=1
cargo test retrieve_tests -- --test-threads=1
cargo fmt --check
git diff --check
```

Manual if feasible:

```bash
cargo run -- smoke <fixture-docset> "middleware example"
```

## Commit/report

Before committing, confirm you are on the U1 branch/worktree.

```bash
git commit -s -m "feat(cli): add smoke command and next-step output (U1)"
```

Final report must include:

1. task: U1
2. commit SHA
3. branch/worktree used
4. files changed
5. tests run and results
6. any open questions for docs/onboarding
