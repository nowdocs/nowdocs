# Rust
/target
**/*.rs.bk
# Cargo.lock 被显式追踪（二进制 crate 应入仓库），此处不设 ignore 规则——勿补回 `Cargo.lock` 忽略行（曾因尾空格 pattern 失配而意外"不忽略"，若改回正常忽略会掩盖 lock 变更）

# 构建产物 / 嵌入模型缓存（绝不入仓库）
*.lance
/model.safetensors
/config.json
/tokenizer.json

# 编辑器 / 系统
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# 本地协作与内部归档（不属于公开仓库）
.nowdocs-internal/
.mimocode/
.workbuddy/
.zcode/
tmp/
clippy_job.log
test.log

# 环境与密钥（三防线：密钥不入库）
.env
.env.*
*.local
secrets/
2a-test.log
2b-test.log
2b-test-full.log
2b-build.log
2c-*.log
3a-*.log
3b-*.log
4*.log
5*.log

# 子代理隔离 worktree（临时，勿入库）
.claude/worktrees/
.worktrees/
implementation/worktrees/

# 种子 crate 工作区 — 临时 cloned repos + 调试日志不入库
seed-crates/tmp/*-docs/
seed-crates/tmp/ingest-*.log
seed-crates/tmp/share-*.log
seed-crates/tmp/__pycache__/
seed-crates/tmp/*_rebuilt/

# license 审计工具产物（本地审计报告 / curl 调试 / python 缓存，勿入库）
scripts/audit_license/report.json
scripts/audit_license/__pycache__/
curl_output.txt

# 上游原始文档源（抓取+剥离后的中间产物，可由 share/ 产物中
# manifest.source_url 重新 clone 重建）— 不入库避免版权冗余拷贝+仓库膨胀
seed-crates/src/
