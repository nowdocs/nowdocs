# Rust
/target
**/*.rs.bk
# Cargo.lock 被显式追踪（二进制 crate 应入仓库），此处不设 ignore 规则——勿补回 `Cargo.lock` 忽略行（曾因尾空格 pattern 失配而意外"不忽略"，若改回正常忽略会掩盖 lock 变更）

# 构建产物 / 嵌入模型缓存（绝不入仓库）
*.lance
/model.safetensors
/config.json
/tokenizer.json

# 编辑器 / 系统
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# 环境与密钥（三防线：密钥不入库）
.env
.env.*
*.local
secrets/
2a-test.log
2b-test.log
2b-test-full.log
2b-build.log
2c-*.log
