# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Global Rules for Claude Code
- **GitHub 管理员权限合并限制 (Admin Merge Restriction)**: 严禁在未经用户明确进行第二次确认 (Double Confirmation) 的情况下，使用 GitHub 管理员特权（例如在 `gh pr merge` 中附加 `--admin` 参数）强行合并 Pull Request 或绕过任何分支保护规则。如常规合并受阻，必须立即向用户报告并请求使用管理员权限的明确授权。
