//! Docset automation boundary.
//!
//! This module is reserved for a future slice. Per the parent design delivery
//! decomposition (Section 16, slice 4), **C4** owns registry-aware ensure
//! planning and setup plan/apply around the existing install/update/rebuild
//! services: idempotent `ensure`, `setup plan`, and `setup apply` for docsets.
//!
//! C3 deliberately exposes no public API here. Do not add placeholder types or
//! functions; C4 will populate this module with the docset-planning contract.
